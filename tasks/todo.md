# todo — redesign direction round (2026-07-11)

User decisions from today's direction session:

- [x] Cursor idea: prototype BOTH treatments and let user judge on localhost
      → built `/proto-cursor` (Version A: full crosshair, pointer picks the
      domain image; Version B: default cursor, scene leans toward pointer).
      Demo Anduril-style nav on that page: Space/Air/Sea left, Manifesto ·
      Consulting · Apply right. AWAITING USER VERDICT.
- [ ] Manifesto: dedicated /manifesto page (morality of AI warfare — precision
      reduces civilian deaths, deterrence prevents wars, builders should be the
      ones thinking hardest about ethics) + one distilled line inside the
      journey. USER-APPROVED.
- [ ] Consulting program: own page + flagship (top, visually larger) row in
      "What we do" — currently row 03 "Government & Industry Projects".
      Rename to lead with consulting. USER-APPROVED.
- [ ] Nav: Manifesto, Consulting, Apply confirmed. About DROPPED from nav
      (homepage carries the about story). Anduril-style domain links = user
      curious but undecided — the /proto-cursor demo nav shows the fit
      (domains → existing /domains/* pages). AWAITING USER VERDICT.
- [ ] Finish + ship the one-shot journey (the existing locked direction) —
      still the single highest-impact "make it cool" item.

## Review

(unfilled — work in flight)
